// Stub for the `"DuetWebControl/components"` public component palette. Real DWC re-exports every
// globally-registered component by name (see build-plugin.js's generateApiTypings); this stub only
// covers components a consumer plugin actually imports from that path - add more as needed.
export { default as FirmwareUpdateDialog } from "./FirmwareUpdateDialog.vue";
export { default as ConfigUpdatedDialog } from "./ConfigUpdatedDialog.vue";
